#include <iostream>
#include <vector>
using namespace std;

pair<int,int> findJewel(const vector<vector<int>>& matrix, int target) {
    int n = matrix.size();
    int m = matrix[0].size();

    int row = 0, col = m - 1;
    pair<int,int> result = {-1, -1};

    while (row < n && col >= 0) {
        if (matrix[row][col] == target) {
            result = {row, col};
            col--; 
        } else if (matrix[row][col] > target) {
            col--; 
        } else {
            row++; 
        }
    }

    return result;
}

int main() {
    int n, m, target;
    cin >> n >> m;
    vector<vector<int>> matrix(n, vector<int>(m));
    
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            cin >> matrix[i][j];

    cin >> target;

    pair<int,int> pos = findJewel(matrix, target);
    cout << "(" << pos.first << "," << pos.second << ")" << endl;
    return 0;
}
